#include <stdio.h>

/*
 * main: prints (using printf) - with proper grammar, but the outcome is a piece of art,
 * Return: 0 for successful exit, non-zero for unsuccessful exit
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
