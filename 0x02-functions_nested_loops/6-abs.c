#include "main.h"

/**
 * _abs - accepts integer and returns its absolute value
 * @n: variable is an integer
 * Return: absolute value of provided integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
